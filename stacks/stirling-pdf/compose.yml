name: ${PROJECT}

networks:
  proxy:
    external: true
  default:
    external: true
    name: proxy

services:
  stirling-pdf:
    container_name: hl-stirling-pdf
    image: frooodle/s-pdf:latest
    environment:
      - DOCKER_ENABLE_SECURITY=false
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "1"
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    volumes:
      - ${VOLUMES_PATH}/stirling-pdf/data:/usr/share/tesseract-ocr/4.00/tessdata:z
      - ${VOLUMES_PATH}/stirling-pdf/config:/configs:z
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.services.hl-stirling-pdf.loadbalancer.server.port=8080"
      - "traefik.http.routers.hl-stirling-pdf.rule=Host(`tools.${DOMAIN}`)"
      - "traefik.http.routers.hl-stirling-pdf.entrypoints=websecure"
      - "traefik.http.routers.hl-stirling-pdf.tls=true"
      - "traefik.http.routers.hl-stirling-pdf.middlewares=authelia@file,robots-deny@file"
      - "traefik.http.routers.hl-stirling-pdf.tls.certresolver=myresolver"
