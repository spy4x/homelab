{
  "tasks": {
    // #region Ansible
    "ansible": {
      "command": "deno run -A ./scripts/ansible/+main.ts",
      "description": "🔧 Run Ansible playbooks (auto-loads env vars)"
    },
    // #endregion Ansible

    // #region Deploy
    "deploy": {
      "command": "deno run -A ./scripts/deploy/+main.ts",
      "description": "🚀 Deploy services to remote server (compose.yml, localStacks, sharedStacks)"
    },
    "ssh": {
      "command": "deno run -A ./scripts/ssh/+main.ts",
      "description": "🔐 SSH into server by name"
    },
    // #endregion Deploy

    // #region Backup
    "backup": {
      "command": "deno run --env-file=.env -A ./scripts/backup/+main.ts",
      "description": "💾 Run backup for all services using Restic"
    },
    "backup:restore": {
      "command": "deno run -A ./scripts/backup/restore.ts",
      "description": "📥 Restore service data from backup (interactive)"
    },
    "offline-backup": {
      "command": "deno run --env-file=.env -A ./scripts/offline-backup/+main.ts",
      "description": "🗄️ Backup/restore to/from external drive (monthly offline backups)"
    },
    // #endregion Backup

    // #region Encryption
    "encrypt:init": {
      "command": "deno run -A ./scripts/encryption/+main.ts init --server",
      "description": "🔐 Initialize encryption for server (generate keys, create config)"
    },
    "encrypt:env": {
      "command": "deno run -A ./scripts/encryption/+main.ts env --server",
      "description": "🔒 Encrypt .env file to .env.age using SOPS + age"
    },
    "decrypt:env": {
      "command": "deno run -A ./scripts/encryption/decrypt.ts",
      "description": "🔓 Decrypt .env.age file to .env using SOPS + age"
    },
    "encrypt:root:init": {
      "command": "deno run -A ./scripts/encryption/root.ts init",
      "description": "🔐 Initialize encryption for root .env.root"
    },
    "encrypt:root": {
      "command": "deno run -A ./scripts/encryption/root.ts encrypt",
      "description": "🔒 Encrypt .env.root to .env.root.age"
    },
    "decrypt:root": {
      "command": "deno run -A ./scripts/encryption/root.ts decrypt",
      "description": "🔓 Decrypt .env.root.age to .env.root"
    },
    // #endregion Encryption

    // #region Checks
    "check": {
      "command": "echo '✅ All checks passed!'",
      "description": "✅ Run all checks (lint, fmt, types, tests)",
      "dependencies": [
        "lint:check",
        "fmt:check",
        "ts:check",
        "test"
      ]
    },
    "fmt:fix": {
      "command": "deno fmt",
      "description": "✨ Auto-format code"
    },
    "fmt:check": {
      "command": "deno fmt --check",
      "description": "📝 Check code formatting"
    },
    "lint:fix": {
      "command": "deno lint --fix",
      "description": "🔍 Auto-fix linting issues"
    },
    "lint:check": {
      "command": "deno lint",
      "description": "🔍 Check for linting issues"
    },
    "ts:check": {
      "command": "deno check **/*.ts",
      "description": "📘 Type-check TypeScript files"
    },
    "test": {
      "command": "deno test",
      "description": "🧪 Run tests"
    },
    "fix": {
      "command": "echo '🔧 All fixes applied!'",
      "description": "🔧 Auto-fix all issues (lint + format)",
      "dependencies": [
        "lint:fix",
        "fmt:fix"
      ]
    }
    // #endregion Checks
  },
  "imports": {
    "@scripts/backup": "./scripts/backup/src/+lib.ts",
    "@std/dotenv": "jsr:@std/dotenv",
    "@std/fs": "jsr:@std/fs@^1.0.21",
    "@std/path": "jsr:@std/path@^1.1.4"
  },
  "exclude": [
    "**/.vite/*",
    "**/.cache/*",
    "**/dist/*",
    ".volumes",
    ".git",
    "node_modules",
    "dist",
    "build",
    "docs",
    "infra",
    "*.lock"
  ],
  "lint": {
    "rules": {
      "tags": [
        "recommended"
      ],
      "exclude": [
        "require-await"
      ]
    }
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 100,
    "indentWidth": 2,
    "singleQuote": false,
    "proseWrap": "preserve",
    "semiColons": false
  }
}
