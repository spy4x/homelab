# Example environment variables for Hetzner Cloud Mail Server
# Copy this to .env and fill in your actual values

# Project name
PROJECT=cloud

# Domain configuration
DOMAIN=antonshubin.com
CONTACT_EMAIL=2spy4x@gmail.com
TIMEZONE=Asia/Singapore

# Basic authentication for Traefik dashboard and Rspamd
# Generate with: echo $(htpasswd -nb user password) | sed 's/\$/\$\$/g'
BASIC_AUTH_USER=user
BASIC_AUTH_PASSWORD=$$apr1$$replace$$with$$your$$hash

# Mail Server Configuration
# Note: Email accounts are managed via docker exec commands (see README.md)
# SMTP credentials for other services to send emails
SMTP_HOST=mail.antonshubin.com
SMTP_PORT=587
SMTP_FROM=noreply@antonshubin.com
SMTP_USERNAME=noreply@antonshubin.com
SMTP_PASSWORD=REPLACE_WITH_GENERATED_PASSWORD

# Healthchecks - Cron job monitoring
# Generate with: head -c 32 /dev/urandom | base64 | tr -d '=' | head -c 64
HEALTHCHECKS_SECRET_KEY=REPLACE_WITH_RANDOM_64_CHAR_STRING

# Backup Configuration (optional - if you want to backup the mail server)
BACKUPS_PASSWORD=REPLACE_WITH_YOUR_SECURE_PASSWORD
NTFY_URL=https://ntfy.antonshubin.com/backups
NTFY_AUTH_TOKEN=tk_your_token_here
SLACK_WEBHOOK_URL=YOUR_SLACK_WEBHOOK_URL_FOR_NOTIFICATIONS
